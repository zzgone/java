package 流IO;

public class File获取文件信息 {

	/**
	 * File类的常用方法
	 * getName();			获取文件的名称
	 * canRead();			判断文件是否是可读的
	 * canWrite();			判断文件是否可被写入
	 * exits();				判断文件是否存在
	 * length();			获取文件的长度（以字节为单位）
	 * getAbsolutePath();	获取文件的绝对路径
	 * getParent();			获取文件的父路径
	 * isFile();			判断文件是否存在
	 * isDirectory();		判断文件是否是一个目录
	 * isHidden();			判断文件是否是隐藏文件
	 * lastModified();		获取文件最后修改时间
	 * 
	 * */
}
